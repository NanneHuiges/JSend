#!/usr/bin/env bash

cd "${BASH_SOURCE%/*}/.." || exit

if [[ $# -eq 0 ]]; then
  docker-compose run --rm jsend-dev-82 vendor/bin/phpunit
else
  docker-compose run --rm jsend-dev-82 composer "${@:1}"
fi
